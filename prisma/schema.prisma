datasource db {
  provider = "postgresql"
  url      = "postgres://root:Z0jckVwwbi1pogBanDdaljOhKlsxWIXu@dpg-cjn251the99c73db4qlg-a.singapore-postgres.render.com/root_3rqy"
}

generator client {
  provider = "prisma-client-js"
}

model Repos {
  id           Int      @id @default(autoincrement())
  thumbnailUrl String
  title        String
  description  String   @unique
  subBranch    String   @unique
  version      String
  liveLink     String
  category     Category @relation(fields: [categoryId], references: [id])
  tags         String
  isPublished  Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  // Add the repoId field to complete the relationship
  categoryId   Int
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  name      String
  role      String   @default("user")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  // Define a relationship to Repos if needed
}

model Category {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  repos     Repos[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
